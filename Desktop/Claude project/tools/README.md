# Tools

Python scripts for deterministic execution. Each script should:
- Do one thing well
- Accept inputs via CLI args or environment variables
- Print clear output and errors
- Be independently testable

Credentials and API keys are loaded from `.env` in the project root.
